node_modules/
npm-debug.log
.DS_Store
.env
.env.*
*.log
.claude/
# Top-level only — leaves public/data/ (the world-110m.json static asset)
# committable. The local SQLite DB lives in /data/ via DATA_DIR env var.
/data/
